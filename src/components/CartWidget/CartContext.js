/*
con su context y custom provider (importalo en app.js)

al clickear comprar en item detail se debe guardar en cart context el producto y la cantidad en forma de objeto {item: {} quantity}

logica de no aceptar duplicados 

metodos recomendados

addItem (item quantity)
removeitem(itemID)
clear()
isincart: (id) => true/false
*/

/**
function itemDetail( { item } ){
    onAdd(quantityToAdd)
    //revibido un evento del itemcount
}
return<>
    <itemCount> //configura las props de itemcount
    <button> termina tu compra</button> //oculto hasta que el usuario seleccione la catidad y clickee en comprar
</>

 */